super + w
        $BROWSER

super + e
        /opt/JetBrains-Rider-2025.1/bin/rider.sh


super + d
        dmenu_run

Print
        maim -u -i $(xdotool getactivewindow) | xclip -selection clipboard -t image/png

super + S
	      dbeaver

ctrl + Print
        maim -s | xclip -selection clipboard -t image/png

super + Print
        $HOME/.local/bin/screencast.sh

super + p
        mpv $(xclip -o)

super + i
      	ILSpy

super + Return
        $TERMINAL

super + s
        center-term.sh

super + {equal, minus, BackSpace}
    	  volume.sh {up, down, toggle}

super + m
        $HOME/.local/bin/mount.sh

super + t
        /opt/tor-browser/launch_tor.sh

ctrl + shift 1
      	connect_soundcore.sh

ctrl + shift 2
      	connect_huawei.sh

super + shift + BackSpace
        $HOME/.local/bin/exit.sh

super + a
        libreoffice

super + n
        $TERMINAL -e  nnn -Rr

super + z
        sleep.sh

super + c
        code

super + o
        mypaint

super + r
        /opt/Postman/Postman
alt + w
        connect_wifi.sh

alt + shift + w
        disconnect_wifi.sh

super + g
        dbus-launch --exit-with-session flatpak run org.geogebra.GeoGebra

